const text=async(file)=>await(await file.getFile()).text();
async function dir(root,name){try{return await root.getDirectoryHandle(name)}catch{return null}}
async function file(root,path){let h=root;for(const p of path.split('/').slice(0,-1))h=await h.getDirectoryHandle(p);return h.getFileHandle(path.split('/').pop())}
async function read(root,path){try{return text(await file(root,path))}catch{return ''}}
function cleanRef(ref){return ref.trim().replace(/^ref:\s*/,'')}
export async function readGitRepository(root,files=[]){const g=await dir(root,'.git');if(!g)return{available:false};const headRaw=await read(g,'HEAD');const head=cleanRef(headRaw);let branch=headRaw.startsWith('ref:')?head.split('/').pop():null;const config=await read(g,'config');const remote=(config.match(/\[remote "origin"\][\s\S]*?url\s*=\s*(.+)/)||[])[1]?.trim()||'';const packed=await read(g,'packed-refs');let resolvedHead=head;if(branch){const direct=await read(g,'refs/heads/'+branch);if(direct.trim())resolvedHead=direct.trim();else{const m=packed.split(/\r?\n/).find(x=>x.endsWith(' refs/heads/'+branch));if(m)resolvedHead=m.split(' ')[0]}}return{available:true,branch,head:resolvedHead,remote}}
function parseIndex(buffer){const bytes=new Uint8Array(buffer);if(new TextDecoder().decode(bytes.slice(0,4))!=='DIRC')return[];const dv=new DataView(buffer);const count=dv.getUint32(8);let pos=12;const out=[];for(let i=0;i<count&&pos+62<=bytes.length;i++){const mtime=dv.getUint32(pos+8),size=dv.getUint32(pos+36),flags=dv.getUint16(pos+60);let end=pos+62;while(end<bytes.length&&bytes[end]!==0)end++;const path=new TextDecoder().decode(bytes.slice(pos+62,end));out.push({path,mtime,size,flags});pos+=Math.ceil((end-pos+1)/8)*8}return out}
export async function gitStatusSummary(root,files=[]){const g=await dir(root,'.git');if(!g)return{available:false};try{const indexFile=await file(g,'index');const entries=parseIndex(await(await indexFile.getFile()).arrayBuffer());const current=new Map(files.map(f=>[f.path,f]));const tracked=new Set(entries.map(x=>x.path));const untracked=files.filter(f=>!tracked.has(f.path)).map(f=>f.path);const deleted=entries.filter(e=>!current.has(e.path)).map(e=>e.path);const byPath=new Map(entries.map(e=>[e.path,e]));const probableModified=[];for(const f of files){const e=byPath.get(f.path);if(e){try{const lf=(await f.handle.getFile()).lastModified/1000;if(Math.abs(lf-e.mtime)>2)probableModified.push(f.path)}catch{}}}return{available:true,modified:[...new Set(probableModified)],untracked,deleted,tracked:entries.length}}catch(e){return{available:true,error:e.message,modified:[],untracked:files.map(f=>f.path),deleted:[],tracked:0}}}
export async function gitActivity(root){const g=await dir(root,'.git');if(!g)return[];const raw=await read(g,'logs/HEAD');return raw.split(/\r?\n/).filter(Boolean).slice(-30).reverse().map(line=>{const m=line.match(/^([0-9a-f]{40})\s+([0-9a-f]{40})\s+(.+?)\s+<[^>]+>\s+(\d+)\s+[+-]\d+\t(.*)$/);return m?{oldHash:m[1],hash:m[2],author:m[3],date:Number(m[4])*1000,message:m[5]}:{hash:line.slice(41,81),action:line.slice(0,180)}})}